export default {
    loader: {
        icon: {
            size: "{loader.icon.size}"
        },
        mask: {
            color: "{mask.color}",
            background: "{mask.background}"
        }
    }
}