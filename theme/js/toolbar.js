export default {
    root: {
        gap: "{toolbar.gap}",
        color: "{warn.color}",
        padding: "{toolbar.padding}",
        background: "{warn.color}",
        borderColor: "{warn.border.color}",
        borderRadius: "{toolbar.border.radius}"
    }
}