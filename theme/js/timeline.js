export default {
    event: {
        minHeight: "{event.min.height}"
    },
    vertical: {
        eventContent: {
            padding: "{vertical.event.content.padding}"
        }
    },
    horizontal: {
        eventContent: {
            padding: "{horizontal.event.content.padding}"
        }
    },
    eventMarker: {
        size: "{event.marker.size}",
        content: {
            size: "{event.marker.content.size}",
            background: "{primary.500}",
            insetShadow: "0 1px 1px 0 #0000001f, 0 1px 0 0 #0000000f",
            borderRadius: "{content.border.radius}"
        },
        background: "{surface.0}",
        borderColor: "{content.border.color}",
        borderWidth: "{event.marker.border.width}",
        borderRadius: "{event.marker.border.radius}"
    },
    eventConnector: {
        size: "{event.connector.size}",
        color: "{mask.color}"
    }
}